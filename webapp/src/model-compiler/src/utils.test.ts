import { expect } from '@jest/globals';

describe('utils', () => {
  describe('iterateTopologically', () => {
    it('iterates in correct forward order', () => {});

    it('iterates in correct backward order', () => {});
  });
});
